# Kent Demo Bot — Переменные окружения
# Минимальный набор для запуска демо-бота

OPENCLAW_GATEWAY_TOKEN=          # openssl rand -hex 16
OPENCLAW_HOOKS_TOKEN=            # openssl rand -hex 16
DEMO_TELEGRAM_BOT_TOKEN=         # Отдельный токен от @BotFather для демо-бота
